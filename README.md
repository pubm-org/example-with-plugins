# example-with-plugins

pubm example: single package using `@pubm/plugin-brew` and `@pubm/plugin-external-version-sync`.

## Configuration

See [pubm.config.ts](./pubm.config.ts) for the configuration.
